export default class User{
    constructor(id, user_id, title, description, status, done_date){
        id,
        user_id,
        title,
        description,
        status,
        done_date
    }
}